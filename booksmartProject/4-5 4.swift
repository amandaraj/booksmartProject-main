//
//  4-5.swift
//  booksmartProject
//
//  Created by Amanda on 7/22/21.
//

import UIKit

class __5: UIViewController {
    
    
    @IBAction func bigNateMakesTheGrade(_ sender: Any) {
        if let url = URL(string: "https://www.youtube.com/watch?v=GQelRB_faYM&list=PLXSWZxwdp49KHDos7gTP8xgmu8mixqmDt&index=37") {
              UIApplication.shared.open(url, options: [:], completionHandler: nil)
    }
    }
    
    @IBAction func theLittlePrince(_ sender: Any) {
        if let url = URL(string: "https://www.youtube.com/watch?v=yWQo_AAHDUA") {
              UIApplication.shared.open(url, options: [:], completionHandler: nil)
    }
    }
    
    @IBAction func charlottesWeb(_ sender: Any) {
        if let url = URL(string: "https://www.youtube.com/watch?v=9jWsZ3PV2v0&list=PLNWB9LCZQNVw-ATvXDiS4IGclU2mwfuSN") {
              UIApplication.shared.open(url, options: [:], completionHandler: nil)
    }
    }
    
    @IBAction func wonder(_ sender: Any) {
        if let url = URL(string: "https://www.youtube.com/watch?v=N9XilBZFVsg") {
              UIApplication.shared.open(url, options: [:], completionHandler: nil)
    }
    }
    
    
    @IBAction func sherlockHolmes(_ sender: Any) {
        if let url = URL(string: "https://www.youtube.com/watch?v=be_gqA3nF70") {
              UIApplication.shared.open(url, options: [:], completionHandler: nil)
    }
    }
    
    
    @IBAction func oneAndOnlyIvan(_ sender: Any) {
        if let url = URL(string: "https://www.youtube.com/watch?v=h6oi3PZc2ko") {
              UIApplication.shared.open(url, options: [:], completionHandler: nil)
    }
    }
    
    @IBAction func ELA(_ sender: Any) {
        if let url = URL(string: "https://www.ixl.com/ela/grade-4") {
              UIApplication.shared.open(url, options: [:], completionHandler: nil)
    }
    }
    @IBAction func fourmath(_ sender: Any) {
        if let url = URL(string: "https://www.ixl.com/math/grade-4") {
              UIApplication.shared.open(url, options: [:], completionHandler: nil)
    }
    }
    @IBAction func fourfunbrain(_ sender: Any) {
        if let url = URL(string: "https://www.funbrain.com/grade/4") {
              UIApplication.shared.open(url, options: [:], completionHandler: nil)
    }
    }
    @IBAction func fourabcya(_ sender: Any) {
        if let url = URL(string: "https://www.abcya.com/grades/4") {
              UIApplication.shared.open(url, options: [:], completionHandler: nil)
    }
    }
    @IBAction func fifthela(_ sender: Any) {
        if let url = URL(string: "https://www.ixl.com/ela/grade-5") {
              UIApplication.shared.open(url, options: [:], completionHandler: nil)
    }
    }
    @IBAction func fifthmath(_ sender: Any) {
        if let url = URL(string: "https://www.ixl.com/math/grade-5") {
              UIApplication.shared.open(url, options: [:], completionHandler: nil)
    }
    }
    @IBAction func fifthfunbrain(_ sender: Any) {
        if let url = URL(string: "https://www.funbrain.com/grade/5") {
              UIApplication.shared.open(url, options: [:], completionHandler: nil)
    }
    }
    @IBAction func fifthabcya(_ sender: Any) {
        if let url = URL(string: "https://www.abcya.com/grades/5") {
              UIApplication.shared.open(url, options: [:], completionHandler: nil)
    }
    }
    
    @IBAction func wrinkleintime(_ sender: Any) {
        if let url = URL(string: "http://albanycomplementaryhealth.com/wp-content/uploads/2016/07/A-Wrinkle-in-Time-Madeleine-LEngle.pdf") {
              UIApplication.shared.open(url, options: [:], completionHandler: nil)
    }
    }
    @IBAction func charliechocolate(_ sender: Any) {
        if let url = URL(string: "http://www.bratton.wilts.sch.uk/wp-content/uploads/2017/09/Charlie-and-the-Chocolate-Factory.pdf") {
              UIApplication.shared.open(url, options: [:], completionHandler: nil)
    }
    }
    @IBAction func maniacmagee(_ sender: Any) {
        if let url = URL(string: "https://archive.org/download/ManiacMagee-English-JerryS/maniacmagee.pdf") {
              UIApplication.shared.open(url, options: [:], completionHandler: nil)
    }
    }
    @IBAction func seriesunfortunate(_ sender: Any) {
        if let url = URL(string: "http://readster.weebly.com/uploads/9/7/9/4/97949932/13_the_end.pdf") {
              UIApplication.shared.open(url, options: [:], completionHandler: nil)
    }
    }
    @IBAction func redpyramid(_ sender: Any) {
        if let url = URL(string: "https://thedshow.files.wordpress.com/2013/03/1-the-red-pyramid.pdf") {
              UIApplication.shared.open(url, options: [:], completionHandler: nil)
    }
    }
    @IBAction func treasureisland(_ sender: Any) {
        if let url = URL(string: "https://www1.udel.edu/LLL/language/deutsch/handouts/summer_2015/Schatzinsel_E.pdf") {
              UIApplication.shared.open(url, options: [:], completionHandler: nil)
    }
    }
    override func viewDidLoad() {
        super.viewDidLoad()

        // Do any additional setup after loading the view.
    }
    

    /*
    // MARK: - Navigation

    // In a storyboard-based application, you will often want to do a little preparation before navigation
    override func prepare(for segue: UIStoryboardSegue, sender: Any?) {
        // Get the new view controller using segue.destination.
        // Pass the selected object to the new view controller.
    }
    */

}

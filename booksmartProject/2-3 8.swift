//
//  2-3.swift
//  booksmartProject
//
//  Created by Amanda on 7/22/21.
//

import UIKit

class __3: UIViewController {
    
    @IBAction func aliceInWonderland(_ sender: Any) {
        if let url = URL(string: "https://www.youtube.com/watch?v=KMVi_Q8izJU&list=OLAK5uy_lPfnQDmz78DJRTe8KvunvXj2tivD1C-M8&index=7") {
              UIApplication.shared.open(url, options: [:], completionHandler: nil)
    }
    }
    
    @IBAction func asFastAsWordsCouldFly(_ sender: Any) {
        if let url = URL(string: "https://www.youtube.com/watch?v=1_Zk4P50WT4") {
              UIApplication.shared.open(url, options: [:], completionHandler: nil)
    }
    }
    
    @IBAction func clementine(_ sender: Any) {
        if let url = URL(string: "https://www.youtube.com/watch?v=0kMyCPtmI2c") {
              UIApplication.shared.open(url, options: [:], completionHandler: nil)
    }
    }
    
    @IBAction func jamesAndTheGiantPeach(_ sender: Any) {
        if let url = URL(string: "https://www.youtube.com/watch?v=gFoYBO5ZFwo") {
              UIApplication.shared.open(url, options: [:], completionHandler: nil)
    }
    }
    
    @IBAction func heidi(_ sender: Any) {
        if let url = URL(string: "https://www.youtube.com/watch?v=fnpM-8tNeQs&list=PLXSWZxwdp49KHDos7gTP8xgmu8mixqmDt&index=30") {
              UIApplication.shared.open(url, options: [:], completionHandler: nil)
        }
    }
    
    
    @IBAction func theCaseOfTheMissingCarrotCake(_ sender: Any) {
        if let url = URL(string: "https://www.youtube.com/watch?v=-UO-fLPG1kg") {
              UIApplication.shared.open(url, options: [:], completionHandler: nil)
    }
    }
    
    
    @IBAction func secondLangArts(_ sender: Any) {
        if let url = URL(string: "https://www.ixl.com/ela/grade-2") {
              UIApplication.shared.open(url, options: [:], completionHandler: nil)
    }
    }
    
    
    @IBAction func secondMath(_ sender: Any) {
        if let url = URL(string: "https://www.ixl.com/math/grade-2") {
              UIApplication.shared.open(url, options: [:], completionHandler: nil)
    }
    }
    
    
    @IBAction func funbrainSecond(_ sender: Any) {
        if let url = URL(string: "https://www.funbrain.com/grade/2") {
              UIApplication.shared.open(url, options: [:], completionHandler: nil)
    }
    }
    
    
    @IBAction func abcyaSecond(_ sender: Any) {
        if let url = URL(string: "https://www.abcya.com/grades/2") {
              UIApplication.shared.open(url, options: [:], completionHandler: nil)
    }
    }
    
    @IBAction func thirdLangArts(_ sender: Any) {
        if let url = URL(string: "https://www.ixl.com/ela/grade-3") {
              UIApplication.shared.open(url, options: [:], completionHandler: nil)
    }
    }
    
    @IBAction func thirdMath(_ sender: Any) {
        if let url = URL(string: "https://www.ixl.com/math/grade-3") {
              UIApplication.shared.open(url, options: [:], completionHandler: nil)
    }
    }
    
    
    @IBAction func funbrainThird(_ sender: Any) {
        if let url = URL(string: "https://www.funbrain.com/grade/3") {
              UIApplication.shared.open(url, options: [:], completionHandler: nil)
    }
    }
    
    
    
    @IBAction func abcyaThird(_ sender: Any) {
        if let url = URL(string: "https://www.abcya.com/grades/3") {
              UIApplication.shared.open(url, options: [:], completionHandler: nil)
    }
    }
    
   
    @IBAction func aliceinwonderland(_ sender: Any) {
        if let url = URL(string: "https://www.adobe.com/be_en/active-use/pdf/Alice_in_Wonderland.pdf") {
              UIApplication.shared.open(url, options: [:], completionHandler: nil)
    }
    }
    @IBAction func ramonaandbeezus(_ sender: Any) {
        if let url = URL(string: "https://ebook-mecca.com/online/Beverly%20Cleary%20-%20Beezus%20and%20Ramona%20.PDF") {
              UIApplication.shared.open(url, options: [:], completionHandler: nil)
    }
    }
    @IBAction func popperspenguins(_ sender: Any) {
        if let url = URL(string: "https://amitchelldpa.webs.com/Mr%20Poppers%20Penguins%20The%20Book.pdf") {
              UIApplication.shared.open(url, options: [:], completionHandler: nil)
    }
    }
    @IBAction func narnia(_ sender: Any) {
        if let url = URL(string: "https://s3.amazonaws.com/scschoolfiles/112/thechroniclesofnarnia.pdf") {
              UIApplication.shared.open(url, options: [:], completionHandler: nil)
    }
    }
    @IBAction func boxcarchildren(_ sender: Any) {
        if let url = URL(string: "https://www.gutenberg.org/files/42796/42796-h/42796-h.htm") {
              UIApplication.shared.open(url, options: [:], completionHandler: nil)
    }
    }
    @IBAction func littlehouseinwoods(_ sender: Any) {
        if let url = URL(string: "https://www.gutenberg.ca/ebooks/wildersewell-woods/wildersewell-woods-01-h-dir/wildersewell-woods-01-h.html") {
              UIApplication.shared.open(url, options: [:], completionHandler: nil)
    }
    }
    override func viewDidLoad() {
        super.viewDidLoad()

        // Do any additional setup after loading the view.
    }
    }

    /*
    // MARK: - Navigation

    // In a storyboard-based application, you will often want to do a little preparation before navigation
    override func prepare(for segue: UIStoryboardSegue, sender: Any?) {
        // Get the new view controller using segue.destination.
        // Pass the selected object to the new view controller.
    }
    */



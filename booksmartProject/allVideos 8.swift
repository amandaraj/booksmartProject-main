//
//  allVideos.swift
//  booksmartProject
//
//  Created by Amanda on 7/22/21.
//

import UIKit

class allVideos: UIViewController {

    
    @IBAction func cookiemouse(_ sender: Any) {
        if let url = URL(string: "https://www.youtube.com/watch?v=bzw0pDLAf7k") {
              UIApplication.shared.open(url, options: [:], completionHandler: nil)
        }
    }
    
    @IBAction func peterabbit(_ sender: Any) {
        if let url = URL(string: "https://www.youtube.com/watch?v=p_LeUSA-qIg") {
              UIApplication.shared.open(url, options: [:], completionHandler: nil)
        }
    }
    @IBAction func sylvestermagicpebble(_ sender: Any) {
        if let url = URL(string: "https://www.youtube.com/watch?v=BNGUnk8JeLE") {
              UIApplication.shared.open(url, options: [:], completionHandler: nil)
        }
    }
    @IBAction func emptypot(_ sender: Any) {
        if let url = URL(string: "https://www.youtube.com/watch?v=a9K-sAKdk2Y") {
              UIApplication.shared.open(url, options: [:], completionHandler: nil)
        }
    }
    @IBAction func hanukkahalaska(_ sender: Any) {
        if let url = URL(string: "https://www.youtube.com/watch?v=0Brmv6DHSdc") {
              UIApplication.shared.open(url, options: [:], completionHandler: nil)
        }
    }
    @IBAction func clarkshark(_ sender: Any) {
        if let url = URL(string: "https://www.youtube.com/watch?v=lgZRMptA0Lk") {
              UIApplication.shared.open(url, options: [:], completionHandler: nil)
        }
    }
    @IBAction func alicewonderland(_ sender: Any) {
        if let url = URL(string: "https://www.youtube.com/watch?v=KMVi_Q8izJU&list=OLAK5uy_lPfnQDmz78DJRTe8KvunvXj2tivD1C-M8&index=7") {
              UIApplication.shared.open(url, options: [:], completionHandler: nil)
    }
    }
    @IBAction func fastwordscouldfly(_ sender: Any) {
        if let url = URL(string: "https://www.youtube.com/watch?v=1_Zk4P50WT4") {
              UIApplication.shared.open(url, options: [:], completionHandler: nil)
    }
    }
    @IBAction func clementine(_ sender: Any) {
        if let url = URL(string: "https://www.youtube.com/watch?v=0kMyCPtmI2c") {
              UIApplication.shared.open(url, options: [:], completionHandler: nil)
    }
    }
    @IBAction func jamesgiantpeach(_ sender: Any) {
        if let url = URL(string: "https://www.youtube.com/watch?v=gFoYBO5ZFwo") {
              UIApplication.shared.open(url, options: [:], completionHandler: nil)
    }
    }
    @IBAction func heidi(_ sender: Any) {
        if let url = URL(string: "https://www.youtube.com/watch?v=fnpM-8tNeQs&list=PLXSWZxwdp49KHDos7gTP8xgmu8mixqmDt&index=30") {
              UIApplication.shared.open(url, options: [:], completionHandler: nil)
        }
    }
    @IBAction func carrotcake(_ sender: Any) {
        if let url = URL(string: "https://www.youtube.com/watch?v=-UO-fLPG1kg") {
              UIApplication.shared.open(url, options: [:], completionHandler: nil)
    }
    }
    @IBAction func bignate(_ sender: Any) {
        if let url = URL(string: "https://www.youtube.com/watch?v=GQelRB_faYM&list=PLXSWZxwdp49KHDos7gTP8xgmu8mixqmDt&index=37") {
              UIApplication.shared.open(url, options: [:], completionHandler: nil)
    }
    }
    @IBAction func littleprince(_ sender: Any) {
        if let url = URL(string: "https://www.youtube.com/watch?v=yWQo_AAHDUA") {
              UIApplication.shared.open(url, options: [:], completionHandler: nil)
    }
    }
    @IBAction func charlottesweb(_ sender: Any) {
        if let url = URL(string: "https://www.youtube.com/watch?v=9jWsZ3PV2v0&list=PLNWB9LCZQNVw-ATvXDiS4IGclU2mwfuSN") {
              UIApplication.shared.open(url, options: [:], completionHandler: nil)
    }
    }
    @IBAction func wonder(_ sender: Any) {
        if let url = URL(string: "https://www.youtube.com/watch?v=N9XilBZFVsg") {
              UIApplication.shared.open(url, options: [:], completionHandler: nil)
    }
    }
    @IBAction func sherlockholmes(_ sender: Any) {
        if let url = URL(string: "https://www.youtube.com/watch?v=be_gqA3nF70") {
              UIApplication.shared.open(url, options: [:], completionHandler: nil)
    }
    }
    @IBAction func oneandonlyivan(_ sender: Any) {
        if let url = URL(string: "https://www.youtube.com/watch?v=h6oi3PZc2ko") {
              UIApplication.shared.open(url, options: [:], completionHandler: nil)
    }
    }
    @IBAction func chestersway(_ sender: Any) {
        if let url = URL(string: "https://www.youtube.com/watch?v=iC8YpmnzbU8") {
              UIApplication.shared.open(url, options: [:], completionHandler: nil)
        }
    }
    @IBAction func secretwolfdiary(_ sender: Any) {
        if let url = URL(string: "https://www.youtube.com/watch?v=yqsJ0ZCg0t8") {
              UIApplication.shared.open(url, options: [:], completionHandler: nil)
        }
    }
    
    override func viewDidLoad() {
        super.viewDidLoad()

        // Do any additional setup after loading the view.
    }
    

    /*
    // MARK: - Navigation

    // In a storyboard-based application, you will often want to do a little preparation before navigation
    override func prepare(for segue: UIStoryboardSegue, sender: Any?) {
        // Get the new view controller using segue.destination.
        // Pass the selected object to the new view controller.
    }
    */

}
